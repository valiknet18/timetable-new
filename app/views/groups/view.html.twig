{% extends 'app.html.twig' %}

{% block content %}
    <div>
        <div>
            Назва групи: {{ group.group_name }}
        </div>

        <div>
            Рік вступу: {{ group.group_course }}
        </div>

        <div>
            Кількість студентів: {{ group.group_students_count }}
        </div>

        {% include 'events/events_list.html.twig' with {'events' : group.events, 'message': 'Події в цій группі'}%}
    </div>
{% endblock %}